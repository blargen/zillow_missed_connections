<div class="container">
  <div class="row">
    <div class="card bg-light col-sm-12">
      <% if @zip_formatted_correctly && @missing_headers == [] %>
        <img src="compat.jpg" class="rounded d-block" alt="...">
      <% else %>
        <img src="first.jpg" class="rounded d-block" alt="...">
      <% end %>
      <div class="card-deck">
        <div class="card col-sm-4 left">
          <div class="card-body">
            <h5 class="card-title">How Does This Bode For My Zip Code?</h5>
            <h6 class="card-subtitle mb-2 text-muted">All zip codes should be 5 digits</h6>
            <% if @zip_formatted_correctly %>
              <p class="card-text">Ogre Says, "Yo Bruv, it looks like you be looking for love in all the right places!
                Dem
                zip codes are off
                the chain!"</p>
            <% else %>
              <p class="card-text">Ogre says, "Yo Bruv, your zips be straight busted! You gots to make dem look nice wif
                dem 5 digis!"</p>
            <% end %>
          </div>
        </div>
        <div class="col-sm-4 card">
          <h5 class="card-title">Required Headers</h5>
          <ul class="list-group">
            <% @expected_headers.each do |header| %>
              <% if @missing_headers.include?(header) %>
                <li class="list-group-item list-group-item-danger"><%= header %></li>
              <% else %>
                <li class="list-group-item list-group-item-success"><%= header %></li>
              <% end %>
            <% end %>
          </ul>
        </div>
        <div class="col-sm-4 card text-center right">
          <div class="card-body">
            <% if @zip_formatted_correctly && @missing_headers == [] %>
              <h5 class="card-title">I think we have a romantic connection!</h5>
              <p class="card-text">In your case, CSV stands for Created Sexy Values!
                Let's see if we can't set up a date with your file and Zillow!
              </p>
              <button class="btn btn-success">Create Romance</button>
            <% else %>
              <h5 class="card-title">It looks like maybe we got off on the wrong foot.</h5>
              <p class="card-text">Not every single date leads to a connection!
                Let's give it another shot! There's a right csv for everybody!</p>
              <a class="btn btn-danger" href="/" role="button">Let's try this again</a>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>

</div>